# Paderborn Bearing Fault Diagnosis — Python Dependencies
# =========================================================
# Install:  pip install -r requirements.txt
# Environment: ds-py311 (Python 3.11)
# Keep this file in sync with any new imports added to scripts.
# =========================================================

# Core data
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1

# Visualisation
matplotlib==3.9.0
seaborn==0.13.2

# Machine learning
scikit-learn==1.5.0
xgboost==2.1.0
shap==0.44.1
mlflow==2.14.3

# Deep learning (1D-CNN) — install separately; version depends on CUDA availability.
# CPU-only:  pip install torch --index-url https://download.pytorch.org/whl/cpu
# CUDA 12.x: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# Signal processing
PyWavelets==1.9.0

# Dataset download & extraction
requests==2.32.5
tqdm==4.67.3
rarfile==4.2            # fallback extractor; 7-Zip recommended on Windows

# Inference API
fastapi==0.111.0
uvicorn==0.30.1
