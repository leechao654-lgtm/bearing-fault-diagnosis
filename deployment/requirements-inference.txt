# Paderborn Bearing Fault Diagnosis — Inference Service Dependencies
# ==================================================================
# Minimal subset of requirements.txt — training-only packages excluded:
#   torch, jupyter, matplotlib, seaborn, shap
# ==================================================================

# Core data
numpy==2.4.2
pandas==2.2.3
scipy==1.17.0

# Machine learning (model loading + prediction only)
scikit-learn==1.5.0
xgboost==3.2.0

# Signal processing (DSP feature extraction)
PyWavelets==1.9.0

# MLflow model registry (load saved model)
mlflow==3.10.1

# Inference API
fastapi==0.135.1
uvicorn==0.41.0
python-multipart==0.0.20   # required for FastAPI file upload (UploadFile)
